Change the path in "wav.scp" according to your local folder structure.
Stage 1 of run_gmm.sh creates wav.scp ----This script expects train wave files to be inside the folder "train" and test inside "dev".
